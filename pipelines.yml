resources:
  - name: dbp_repo
    type: GitRepo
    configuration:
      gitProvider: zen1                       # <-- replace with your integration
      path: aaragorn2/dbp    # <-- replace with your repository name
      branches:
        include: master
